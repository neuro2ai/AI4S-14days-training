1. 我今天完成了什么
1. PCA降维及找neighbor
2. UMAP及画图调整
2. 我仍然不懂什么
为什么有的代码写obsm，有的写obs？
obs是每个细胞行的表格信息，obsm是每个细胞对应的多维表示，所以给定cell_type写obs，PCA储存在obsm
X_pca这个储存名字是固定的约定俗成的吗？是强制的
3. 今天最重要的3个概念
• 预处理后进行pca：sc.pp.pca(data)，PCA结果写进了adata，具体放在adata.obsm["X_pca"]。shape查询维度（不一定是二维）
• 预处理后构建邻居图：sc.pp.neighbor(adata)为每个细胞寻找相似邻居，构建细胞之间的关系图。
• 计算UMAP二维坐标：sc.tl.umap(adata)根据前面构建好的邻居图，计算每个细胞的UMAP二维坐标。tl是tool。UMAP 是一种可视化降维方法，目的是把细胞结构压到二维平面上，方便看群体关系。结果写入adata.obsm["X_umap"]
• 把UMAP结果画成二维散点图：sc.pl.umap(adata).pl是plotting，表示画图。
• 按细胞类型上色和调点大小：sc.pl.umap(adata, color = "cell_type", size = 80).在这之前需要给细胞这一列加上类型adata.obs["cell_type"] = ["NSC", "NSC", "Neuron", "Neuron", "Glia"]，用print(adata.obs.columns)检查有没有cell_type这一列
• 回答这 3 个检查问题：
    a. PCA 的结果为什么放在 obsm
    adata.X 保存的是细胞 × 基因表达矩阵；PCA 结果虽然仍然是“每个细胞一个表示”，但列已经变成主成分而不是基因，所以应放在存储细胞多维表示的 adata.obsm 中。
    b. UMAP 上每个点代表什么
    UMAP 图上的每个点代表一个细胞，点的位置来自该细胞在低维表示空间中的坐标。
    c. 为什么要先 PCA 再 UMAP
    先 PCA 是为了把高维表达矩阵压缩成更紧凑、噪声更少的表示，并降低计算负担；neighbors 和 UMAP 再基于这个表示去构建细胞关系和二维可视化。
